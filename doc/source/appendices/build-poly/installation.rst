.. _build-poly-install:

Installation
============

The :program:`build_poly` executable is automatically compiled when
GYRE is built, and installed in the :file:`{$GYRE_DIR}/bin` directory
(see the main :ref:`installation` chapter).
