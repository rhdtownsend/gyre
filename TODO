*) Clean up access to data components
   - shut down where necessary
   (mostly done, but needs more work)

*) ivp_t
   - turn into a class?

*) Get force_ad functionality working properly
  *) Must be done in bvp, as that's the only one which can access shoot
     and b/c
  *) Do we actually need a different b/c?

*) Add Coriolis support

*) Add support for ASCII (ADIPLS?) output
   - add write() methods for modes

*) Add support for auto frequency setting

*) Add flexible output support
   (done)

*) Find reference for inertia calculation
   (done)